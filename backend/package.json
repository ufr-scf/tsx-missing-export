{
  "type": "module",
  "devDependencies": {
    "@types/node": "^20.14.11",
    "esbuild": "^0.23.0",
    "tsx": "^4.16.2",
    "typescript": "^5.5.3"
  },
  "scripts": {
    "dev": "tsx src/index.ts",
    "build": "tsc --build && esbuild src/index.ts --outfile=build/index.js --bundle --platform=node --format=esm"
  }
}
